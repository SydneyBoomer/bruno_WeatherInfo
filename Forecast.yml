info:
  name: Forecast
  type: http
  seq: 2

http:
  method: GET
  url: https://api.weather.gov/gridpoints/ILN/37,35/forecast
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
