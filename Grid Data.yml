info:
  name: Grid Data
  type: http
  seq: 4

http:
  method: GET
  url: https://api.weather.gov/gridpoints/ILN/37,35
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
